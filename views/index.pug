extends layout

block content
  h1= title
  p Welcome to #[em LocalLibrary], a very basic Express website developed as a tutorial example on the Mozilla Developer Network.

  if error
    p Error getting dynamic content.
  else
    p The library has the following record counts:

    ul.content
      li.content #[strong Books:] !{data.book_count}
      hr
      li.content #[strong Copies:] !{data.book_instance_count}
      hr
      li.content #[strong Copies available:] !{data.book_instance_available_count}
      hr
      li.content #[strong Authors:] !{data.author_count}
      hr
      li.content #[strong Genres:] !{data.genre_count}